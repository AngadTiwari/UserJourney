package com.example.userjournery.extensions

import android.content.Context

fun Context.isNetworkAvailable(): Boolean {
    //TODO: check whether the network is available or not, to make network call
    return true
}